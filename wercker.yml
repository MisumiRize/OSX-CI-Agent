box: wercker-labs/docker
no-response-timeout: 15
build:
  steps:
    - script:
    - script:
        name: boot and provision
        code: make
    - script:
        name: serverspec test
        code: make test
